export enum State {
    disabled = 'disabled',
    active = 'active',
    error = 'error',
    success = 'success',
    idle = 'idle',
}

export enum InputKind {
    primary = 'primary',
    ghost = 'ghost',
}
